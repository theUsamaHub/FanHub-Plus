@if($paginator->hasPages())<nav class="member-pagination" aria-label="Results pages">@if($paginator->previousPageUrl())<a class="member-button member-button--quiet" href="{{ $paginator->previousPageUrl() }}">← Previous</a>@endif<span>Page {{ $paginator->currentPage() }} of {{ $paginator->lastPage() }}</span>@if($paginator->nextPageUrl())<a class="member-button member-button--quiet" href="{{ $paginator->nextPageUrl() }}">Next →</a>@endif</nav>@endif
