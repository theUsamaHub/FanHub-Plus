@extends('user.layout', ['pageTitle' => 'My Reviews'])
@section('member-content')
<header class="member-page-heading"><p>YOUR PERSPECTIVE</p><h1>Conversations you started.</h1><span>Keep track of your reviews and their moderation status.</span></header><div class="member-list">@forelse($reviews as $review)<article class="member-panel"><span class="member-status">{{ $review->status }}</span><h2>{{ $review->target_label }}</h2><h3>{{ $review->title }}</h3><p class="member-review-body">{{ $review->body }}</p><form method="post" action="{{ route('user.reviews.destroy', $review) }}">@csrf @method('DELETE')<button class="member-text-button">Delete review</button></form></article>@empty<div class="member-panel member-empty"><h2>Have a take? Share it.</h2><p>You can leave a review on stories, media, characters and merchandise.</p><a href="{{ route('public.explore') }}">Find something to review →</a></div>@endforelse</div>@include('user.partials.pagination', ['paginator' => $reviews])
@endsection
